package tdd;

/*public class Sort {
    private String sort;
    public void listOfNumber(int a, int b, int c) {
        if (a <= b && b <= c ) {
            sort = String.format("%a %b %c", a, b, c);
        }
        else {
            if (b <= a && a<= c){
                sort = String.format("%b %a %c", b, a, c);
            }
        else {
                if (c <= a && a <= b) {
                    sort = String.format("%c %a %b", c, a, b);
                } else {
                    if (a <= c && c <= b) {
                        //   sort =
                    }
                }
            }
*/